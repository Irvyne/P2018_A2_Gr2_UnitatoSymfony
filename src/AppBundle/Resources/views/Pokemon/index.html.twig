{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pokemon list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Hp</th>
                <th>Level</th>
                <th>Attack</th>
                <th>Defense</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pokemon_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.hp }}</td>
                <td>{{ entity.level }}</td>
                <td>{{ entity.attack }}</td>
                <td>{{ entity.defense }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pokemon_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pokemon_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pokemon_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
