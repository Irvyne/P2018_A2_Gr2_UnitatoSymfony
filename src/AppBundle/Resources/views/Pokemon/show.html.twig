{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pokemon</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Hp</th>
                <td>{{ entity.hp }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ entity.level }}</td>
            </tr>
            <tr>
                <th>Attack</th>
                <td>{{ entity.attack }}</td>
            </tr>
            <tr>
                <th>Defense</th>
                <td>{{ entity.defense }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pokemon') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pokemon_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
